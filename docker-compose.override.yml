services:
  web:
    build:
      context: .
      target: build
    command: npm run dev -- -p 4173
    ports:
      - "4173:4173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
