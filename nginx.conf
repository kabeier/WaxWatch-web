# Deprecated: this template runs Next.js directly with `next start`/standalone on EC2.
# Security headers are configured in next.config.mjs and validated in CI.
# Keep this file only as a migration marker; do not mount it in production.
